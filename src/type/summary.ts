export enum EDimensionality {
  DAILY = "day",
  WEEKLY = "week",
  MONTHLY = "month",
  YEARLY = "year",
}

export type SummaryItem = {
  amount: number;
  create_time: string;
  name: number;
  icon: string;
};
