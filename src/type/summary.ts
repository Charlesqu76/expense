export enum EDimensionality {
  DAILY = "DAILY",
  WEEKLY = "WEEKLY",
  MONTHLY = "MONTHLY",
  YEARLY = "YEARLY",
}

export type SummaryItem = {
  amount: number;
  create_time: string;
  name: number;
  icon: string;
};
